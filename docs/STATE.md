# STATE

CURRENT MILESTONE: M0 — Scaffolding
LAST SESSION: 2026-08-29 — M0 code portion complete: four autoloads, m0_smoke debug
scene, registered as main scene. `make check` passes; headless boot confirms autoloads
load and the missing-texture warning fires.

## LOCKED (working — propose diffs, do not rewrite)
- src/autoload/EventBus.gd

## IN PROGRESS (safe to edit freely)
- src/autoload/RunState.gd (rewritten at M3)
- src/autoload/ItemDB.gd (rewritten at M4)
- src/autoload/Textures.gd
- scenes/debug/m0_smoke.tscn / .gd

## NEXT TASK
M1 — Player & Room. One hand-built room, player moves/attacks/takes damage/dies,
programmer-art squares only. Do not start until a session is explicitly scoped to it.

## KNOWN GOOD
- `godot --headless --version` → 4.7.1.stable.official.a13da4feb
- `make check` exits 0 against the current repo
- project.godot generated by the engine; config_version is engine-written
- `bash scripts/godot.sh --headless --quit` boots scenes/debug/m0_smoke.tscn (set as
  main scene), all four autoloads resolve, and it prints
  `WARNING: Missing texture: res://assets/sprites/items/does_not_exist.png` from
  Textures.gd — confirms the placeholder path works end to end.
- Keypress → EventBus.karma_shifted → listener print is wired in m0_smoke.gd but
  requires an actual window (WSLg editor/run) to observe; not exercisable headless.

## OPEN ENVIRONMENT ISSUE
`/home/vscode/.config/godot` is root-owned again (recurrence of D-002 — likely reset by
a container rebuild). Doesn't block headless `make check`/`run`, but will reproduce the
editor-launch failure from D-002 if the GUI editor is opened. Not fixed this session
(out of scope for M0 code task); re-apply the D-002 chown fix, or rebuild the devcontainer,
before next editor session.
