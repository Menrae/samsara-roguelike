# STATE

CURRENT MILESTONE: M0 — Scaffolding
LAST SESSION: 2026-08-27 — devcontainer verified (Godot 4.7.1), project.godot generated,
Godot config volume ownership fixed.

## LOCKED (working — propose diffs, do not rewrite)
(none yet)

## IN PROGRESS (safe to edit freely)
(no GDScript exists yet)

## NEXT TASK
M0 code portion. Do not start M1 — no player, no movement, no rooms.

1. src/autoload/EventBus.gd — signal declarations from PROJECT_PLAN §4 ONLY.
   No state, no functions. Item/enemy params stay untyped until M4; comment why.
2. src/autoload/RunState.gd — §4 fields with defaults. Declarations only, no methods.
3. src/autoload/ItemDB.gd — stub: items Dictionary + empty load_all().
4. src/autoload/Textures.gd — convention resolution per §6.
   item_texture(id) → res://assets/sprites/items/{id}.png, cached.
   Missing file → push_warning(full path) + flat magenta ImageTexture (see DECISIONS D-001).
5. scenes/debug/m0_smoke.tscn + script, set as main scene. Keypress emits
   EventBus.karma_shifted, listener in same scene prints it. On ready, call
   Textures.item_texture("does_not_exist") to confirm the warning path works.

Register all four as autoloads in project.godot. Do NOT edit config_version.

Done when: project runs and a keypress prints an EventBus signal.
On completion: mark src/autoload/EventBus.gd LOCKED (that file only — RunState is
rewritten at M3 and ItemDB at M4).

## KNOWN GOOD
- `godot --headless --version` → 4.7.1.stable.official.a13da4feb
- `make check` exits 0 against the current repo
- project.godot generated by the engine; config_version is engine-written
